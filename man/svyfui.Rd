% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svyfui.R
\name{svyfui}
\alias{svyfui}
\title{Survey-weighted Functional on Scalar Regression}
\usage{
svyfui(
  formula,
  data,
  weights = NULL,
  family = gaussian(),
  boot_type = "weighted",
  num_boots = 500,
  nknots_min = NULL,
  seed = 2025,
  conf_level_pw = 0.95,
  conf_level_joint = 0.95,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{formula}{Formula with functional outcome on predictors, e.g. \code{Y ~ X1 + X2}.}

\item{data}{A data frame with functional outcome columns, predictors, weights, etc.}

\item{weights}{Optional bare column name for weights or external weight vector}

\item{family}{Outcome distribution family (e.g., "gaussian", "binomial").}

\item{boot_type}{Bootstrap method: "BRR", "Rao-Wu-Yue-Beaumont", "weighted", "unweighted".}

\item{num_boots}{Number of bootstrap replicates.}

\item{nknots_min}{Minimum number of knots for smoothing (optional).}

\item{seed}{Random seed for reproducibility.}

\item{conf_level_pw}{Confidence level for pointwise confidence intervals (default 0.95).}

\item{conf_level_joint}{Confidence level for joint confidence intervals (default 0.95).}

\item{verbose}{Whether to print messages about progress}

\item{...}{Additional arguments passed to helpers.}
}
\value{
A list with components:
\item{betaHat}{Smoothed coefficient functions}
\item{cis}{Bootstrap confidence intervals}
\item{boots}{Raw bootstrap draws of coefficients}
\item{tidy_df}{Tidy data frame for plotting with columns l (functional domain), beta_hat (estimate),
lower_pw (pointwise lower CI), upper_pw (pointwise upper CI), lower_joint (joint lower CI), upper_joint (joint upper CI),
and var_name (variable name from regression)}
}
\description{
A high-level wrapper for running survey-weighted
function-on-scalar regression with bootstrap inference.
}
\examples{
\donttest{
   fit <- svyfui(Y ~ X, data = svyfosr::sample_df, weights = weight)
   plot(fit)
}
}
